
//////////////////////////////마우스 커서 좌표////////////////////////////////////////
BOOL WINAPI GetCursorPos( _Out_ LPPOINT lpPoint );

//동적할당 한 클래스의 함수를 사용할 때에는 
 if(변수명)
 변수명->함수();

 이렇게 사용합시다

